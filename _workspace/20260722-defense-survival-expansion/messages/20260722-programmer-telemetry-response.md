# Programmer broadcast — offline telemetry defect response

- **From:** Game Programmer
- **To:** all studio roles
- **Feedback requested:** verify event naming and missing observer fields against each owned gate/artifact; report any conflict to the director and programmer.
- **Scope:** Stage 1 FIX telemetry implementation, current → upgraded app labels, export evidence, and explicit QA-D001…D008 disposition.

## Responses

| QA discovery / known defect | response | reason and evidence |
|---|---|---|
| Versioned local JSON emission and sample export were not implemented or collected. | **fixed** | `defense-telemetry.js` now emits schema-v1 bounded offline records; `app.js` observes runtime state and exposes the lobby **진단 내보내기** control. Direct smoke retained 3/dropped 3 records and parsed valid JSON. Evidence: `engineering/defense-telemetry-sample.json`, `ops/telemetry-contract.md`. |
| Input feedback previously reported identical admitted/displayed timestamps without recording an observer sample. | **fixed** | `app.js` now captures input time, invokes the renderer feedback hook, then captures the visible-feedback boundary and emits/records both timestamps plus latency and tick. |
| Full G6 performance proof (30-minute frame/heap/input soak on target devices) is absent. | **deferred** | Implementation supplies bounded 60-frame aggregates and heap probes, but no 30-minute target-device run was performed. G6 remains FIX; telemetry implementation evidence alone must not be promoted to a gate PASS. |
| Expanded-loop occupation, Echo recovery, hazard, seeded spawn-direction, and enemy-policy outcomes need telemetry. | **deferred** | The schema-v1 observer already retains their agreed fields when present, but it does not invent rules events. Runtime emission remains deterministic-simulation-owned and must be verified with the combat implementation lane. |
| QA-D001 — simulation does not consume tactical occupation/extraction/terrain/hazard/spawn variation. | **deferred** | Telemetry is ready to retain objective, occupation, hazard, and spawn-direction fields, but rules emission is CombatEngineer-owned and was not changed in this response. No gate credit is claimed until deterministic simulation evidence passes. |
| QA-D002 — simulation does not consume enemy policies; bosses only pursue. | **deferred** | Telemetry retains stable `policyId`, HP, and boss-TTK fields when emitted. Policy selection, attack, and breach behavior are rules-authority work assigned to CombatEngineer. |
| QA-D003 — five-controller and seed-17 evidence is outside the 45–55% win target. | **deferred** | Post-objective-routing current-head evidence supersedes both earlier 100%-win and pre-routing 0/50 reports: targeted simulation/campaign tests pass 15/23 with 8 failures; the fresh five-archetype × ten-stage seed-17 matrix wins 5/50 (10%), with 0/5 viable archetypes, 21 extractions, 31 items, 59 skills, and 21 boss reaches. This response changes no balance/rule outcomes; G2/runtime PASS remains prohibited. |
| QA-D004 — `void-aegis` / `ward-binder` affect commander integrity while Gate integrity decides defeat. | **deferred** | The growth UI now exposes authored current → upgraded values without reinterpreting them. Effect ownership and defeat-pressure correction remain deterministic-simulation/catalog work. |
| QA-D005 — growth excludes owned skills, so ranks cannot progress; item/synergy/companion values are incomplete. | **deferred** | The player-facing growth and reward cards now display current → upgraded values and telemetry exports those previews, but rank re-offers and complete item/synergy/companion upgrade semantics were not implemented in this app-observer response. |
| QA-D006 — G6 30-minute soak absent. | **deferred** | Same disposition as the G6 row above: probes exist, measurement does not. |
| QA-D007 — fixed SW cache prevents verified rollback. | **deferred** | ReleaseEngineer confirms cache v2 and missing cutscene/telemetry precache are cache-incoherent. Required release-owned fix is SHA-stamped cache naming, full module closure, and clean/already-controlled-client smoke. Evidence: `ops/rollback-runbook.md`, `ops/release-readiness.md`; release is NOT READY. |
| QA-D008 — occupation/extraction/final-stage cinematic beats need simulation event support. | **deferred** | The telemetry observer accepts the new event fields, but animation/cinematic consumers must observe real deterministic events from CombatEngineer; this response invents none. |
| Current-head combat integration regressed extraction, final victory/rewards, active-skill offers, item ordering, and fallback cutscene contracts. | **deferred** | Post-routing QA targeted run passes 15/23 and fails 8/23. Browser and telemetry smokes remain independently green, but cannot offset rules regressions. Exact evidence was routed to CombatEngineer and the director; no failing test was weakened. |
| QA-D009 — occupation recovery clamps authored 4–12/s values to 1/tick (60/s). | **deferred** | The arithmetic is deterministic-simulation-owned and outside this narrowed telemetry/app response. `Math.max(1, Math.trunc(recoveryPerSecond / 60))` destroys the authored band; CombatEngineer must use deterministic fixed-point/tick accumulation and provide evidence. PM correctly marks the current candidate unadopted. |
| APP-G1-01 — lobby summary omitted Echo recovery, occupation, boss containment, and Archive persistence. | **fixed** | `app.js` now uses the NarrativeDirector-approved sentence: Gate defense → Echo recovery/growth → extraction-point occupation/elite binding → boss containment, with companions/rewards retained in the Archive. Current-head syntax and browser journey pass with no errors. |
| APP-G1-02 — story strip used generic `각성 / 방어 / 성장 / 추출 / 종결` beats. | **fixed** | The strip now reads `관문 방어 / 잔향 회수 / 성장 / 점유·추출 / 보스 봉쇄`, matching W-03/W-01/W-04/W-02/W-03. Current-head browser journey remains PASS. |
| Public current → upgraded DOM contract lacked independent proof. | **fixed** | DefenseContractTests reports 1/1 PASS: three `data-pick` choices rendered, every visible rank delta parsed as upgraded = current + 1, and no page/console errors. |
| Telemetry dropped the stable simulation/catalog `policyId` field. | **fixed** | The version-1 allowlist now retains `policyId` by clean cutover; it does not rewrite rules or preserve the unused `policy` spelling. DefenseContractTests observer contract rerun passes 5/5, including `policyId`, objective/occupation/hazard/spawn/recovery/TTK/delta retention, audio fallback/teardown, and digest non-mutation. |
| QA-D010 — extraction can succeed at tick 157 before an elite candidate exists (`candidate: null`, `progress.extracted: 1`). | **deferred** | This is a simulation-state invariant failure outside the telemetry/app observer lane. It permits progression without the canonical elite beat and blocks public-beat loop credibility until CombatEngineer fixes candidate/order validation and supplies a targeted command receipt. |
| QA-D011 — `EXTRACT_ELITE` succeeds remotely without extraction-point proximity/hold. | **deferred** | Input telemetry records the attempt but must not decide validity. Simulation must enforce deterministic spatial occupation/hold before extraction; the current bypass removes movement/occupation decisions and blocks the public beat. |
| QA-D012 — occupation progress reaches 214/180. | **deferred** | Simulation must clamp or otherwise preserve its authored hold contract. Overflow makes objective timing and telemetry interpretation unreliable, so Stage 1/G2/G7 remain blocked. |
| QA-D013 — strict smoke can report pass while the current five-archetype matrix wins only 5/50 (10%). | **deferred** | The strict smoke is not accepted as G2 evidence. Public-beat release remains blocked until the authoritative targeted suite and archetype matrix meet their gates on the same candidate; pre-routing 0/50 remains historical only. |
| QA-D014 — live `WAVE_VARIANT_STARTED` emits `policy` while the stable catalog/snapshot/telemetry contract is `policyId`. | **deferred** | Recorder clean-cutover and synthetic `policyId` proof are fixed, but live sim→recorder integration is 5/6 because the simulation emitter is noncanonical. CombatEngineer must rename the event field to `policyId`; adding a recorder alias would hide contract drift. |
| P2 — telemetry coalesced distinct same-tick `PROJECTILE_IMPACT` / `MOVE` events. | **fixed** | Recorder identity and payload retention now include `projectileId`, `sourceId`, `targetId`, `from`, `to`, `hit`, and `guardedBy`, serialized for object-safe identity. Focused smoke retained two impacts and two moves from one tick, removed all repeat-render duplicates, kept the configured bound, and produced `engineering/defense-telemetry-dedupe-sample.json`. |
| APP-G1-03 — active-skill/growth surfaces render raw developer IDs because `SKILLS` lacks approved names. | **deferred** | App wiring now renders `SKILLS[id]?.name ?? id` in both surfaces without changing IDs/mechanics, and syntax passes. SystemsDesigner’s active catalog freeze permits only Aegis 70→50 during the evidence run, so the eight approved non-balance `name` fields await Main’s separate approval. Public beat still exposes raw IDs until catalog data lands. |
| Operational privacy copy claims Cloudflare analytics and an `Accept` banner that do not exist. | **fixed** | ReleaseEngineer rewrote `privacy.html` for offline-local campaign storage and optional local diagnostic download; it now states no analytics SDK, cookies, accounts, upload, or consent banner and preserves the host-HTTP boundary/back link/Pages allowlist. Evidence: release closure 3/3 plus Chromium file/static copy smoke with no Cloudflare/Accept claims. |

No catalog balance, renderer, audio, test, or release file was changed in this response.

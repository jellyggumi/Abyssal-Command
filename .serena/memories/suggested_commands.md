# Commands
- Install: `npm ci`.
- Serve static app: `python3 -m http.server 4173`; use HTTP, never `file://`.
- Focused verification commands are in `mem:task_completion` and README verification section.
- On Darwin, no project-specific shell compatibility commands are known.